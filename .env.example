# Gemini API key used for all LLM calls (analysis node, retries included)
# Free-tier key: https://aistudio.google.com/apikey
GEMINI_API_KEY=

# GitHub Personal Access Token (fine-grained, scoped to the test repo(s))
# Needs: contents:read, pull-requests:read+write
GITHUB_TOKEN=
